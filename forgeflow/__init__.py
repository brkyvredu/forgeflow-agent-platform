"""ForgeFlow agent package.

The ADK runtime discovers the application in :mod:`forgeflow.agent`. Keeping package import
side-effect free allows deterministic policy tests to run without model credentials.
"""
